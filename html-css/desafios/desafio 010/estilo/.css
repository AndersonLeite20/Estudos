@charset "utf-8";

@import url('https://fonts.googleapis.com/css2? family = Bebas + Neue & display=swap');

@font-face {
     font-family: 'android';
     src: url('/idroid.otf') format ('opentype');
     font-weight: normal;
}

:root{
     --cor0: #c5ebd6;
     --cor1: #83e1ad;
     --cor2: #3ddc84;
     --cor3: #1c7343;
     --cor4: #134428;
     --cor5: #042a17;

     --font-padrao: arial, verdana, helvetica, sans-serif;
     --font-destack: 'Bebas Neue', cursiva;
     --font-android: 'android'
     
}
* {
     margin: 0px;
     padding: 0px;
}
a.externo::after {
     content: "\00a0\1f517";
}



body{
     background-color: var(--cor1);
     font-family:var(--font-padrao);
     text-align: center;
}
header {
     background-image:linear-gradient(to bottom, var(--cor2), var(--cor5));
     min-height: 150px;
     padding-top: 40px;
}
header > h1 {
     color: white;
     font-weight: normal;
     font-size: 3em;
     font-family: var(--font-android);
     margin-top: 30px;
     margin-bottom: 20px;
     text-shadow: 2px 2px 0px rgba(14, 1, 1, 0.675);
}
header > P {
    
     color: white;
     font-size: 1.2em;
     font-family: var(--font-padrao);
     max-width: 35000px;
     margin: auto;
     padding-left: 30px;
     padding-right: 30px;
     padding-bottom: 30px;
     text-shadow: 2px 2px 0px rgba(14, 1, 1, 0.675);
}
nav{
     background-color: var(--cor5);
     padding: 10px;
     box-shadow: 0px 7px 7px rgba(0, 0, 0, 0.945);
     text-align: left;
}
nav > a {
     color: white;
     padding: 11px;
     text-decoration: none;
     font-weight: bold;
     transition-duration: 0.5s;
}
nav > a:hover {
     background-color: var(--cor2);
     color: var(--cor5);
     border-radius: 5px;
}
main {
     background-color: white;
     margin: auto;
     margin-bottom: 30px;
     min-width: 200px;
     max-width: 1400px;
     padding: 20px;
     box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.524);
     border-bottom: left-radius 20px;
     border-bottom: rigth-radius 20px;
} 
main img {
     width: 100%;
}
main img.pequeno {
     max-width: 350px;
     margin: auto;
     display: block;
}
div.video {
     background-color: var(--cor4);
     margin: 5px -20px 30px -20px;
     padding: 10px;
     
}
div.video > iframe {
    
    
}
main h1 {
     color: var(--cor5);
     font-variant:var(--font-padrao);
     text-align: left;
     font-size: 1.9em;
}
main h2 {
     background-image: linear-gradient(to right, var(--cor2),transparent);
     font-family:var(--font-padrao);
     color: var(--cor4);
     text-align: left;
     font-size: 1.5em;
     text-indent: 10px;
     
}
main  p {
     margin: 15px 0px 15px 0px;
     text-align: justify;
     text-indent: 30px;
     font-size: 1.1em;
     line-height: 1.5em;
}
main strong {
     color: var(--cor5);
     font-weight: bold;
     padding: 7px 5px;
}
main a {
     color: var(--cor5);
     background-color: var(--cor0);
     text-decoration: none;
     text-align: bold;
     padding: 7px 5px;
}
main a:hover {
     background-color: var(--cor2);
     color: var(--cor5);
     text-decoration: underline;
}
 aside { 
     background-color: var(--cor1);
     padding: 10px;
     border-radius: 10px;
     box-shadow: 3px 3px 3px rgba(0, 0, 0, 0.565);
     
}
aside h3 {
     background-color: var(--cor4);
     color: white;
     text-align: left;
     padding: 10px;
     margin: -10px -10px 0px -10px;
     border-radius: 10px 10px 0px 0px;
}
aside > ul {
     list-style-type: "\2714\00a0\00a0";
     list-style-position: inside;
     columns: 2;
     text-align: left;
}
footer {
     background-color: var (--cor5);
     color: white; 
     text-align: center;
     font-size: 0.9em;
     padding: 5px;
}
footer p {
     color: white;
     font-weight: bold;
     text-decoration: none;
}
footer a:hover {
     text-decoration: underline;
     color: var(--cor1);
}